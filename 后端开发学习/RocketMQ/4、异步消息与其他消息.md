异步消息需要构建一个sendcallback对象，或者重写匿名内部类
```java
//需要传递一个SendCallBack  
producer.send(message, new SendCallback() {  
    @Override  
    public void onSuccess(SendResult sendResult) {  
        System.out.println(sendResult);  
    }  
  
    @Override  
    public void onException(Throwable throwable) {  
        log.error(String.valueOf(throwable));  
    }  
});
```

其他类型
```java
//单向消息  
producer.sendOneway(message);  

//延迟消息,为消息设置延时等级  
message.setDelayTimeLevel(直接输入数字);
目前可以实现
1s 5s 10s 30s 
1~30min 和 1h 2h

//批量消息
List<Message> msgList = new ArrayList<>();
String msgContent = "hello world yuandongli piliang";
Message message1 = new Message("topic7", "tag1", msgContent.getBytes());
msgList.add(message1); 
Message message2 = new Message("topic7", "tag1", msgContent.getBytes());
msgList.add(message2);   
Message message3 = new Message("topic7", "tag1", msgContent.getBytes());
msgList.add(message3);
producer.send(msgList); 发送集合
    }
}
```