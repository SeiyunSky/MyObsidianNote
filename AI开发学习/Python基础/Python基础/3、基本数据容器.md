### List 列表
```python
[元素,元素,元素]
#查询元素在列表内的索引
list.index(元素)
#插入元素
list.insert(index,元素)
#追加元素
list.append(元素)
list.extend(容器)
#删除元素
list.del[删除指定下标]
list.pop(删除指定下标)
list.remove(元素)
list.clear()
#统计元素数量
list.count(元素)
len(list)
```

### Tuple 元组
```python
(元素,元素,元素)
tuple.index(元素)
tuple.count(元素)
len(tuple)
```

### String 字符串
```python
str.replace(str1,str2)
str.split(str1)
str.strip() #去除首尾空格
str.count(str1)
len(str)
```

### 序列常用操作（切片
```python
序列[起始下标:结束下标:步长]
```