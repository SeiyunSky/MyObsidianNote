**数据类型**：
```python
Number
 - int
 - float
 - complex 复数
 - bool

String

List

Tuple 有序不可变序列，可以理解为Java里的枚举

Set

Dictionary 类似Map
```
**注释**：
```python
# 
""""""
```

**数据类型**：
type(字面量) 查看数据内容

**类型转换**：
```python
int(x)
float(x)
str(x)
```

**运算符**：
```python
//= 取整除 
**= 幂赋值 
```

**字符串定义方式**：
```python
''
""
""""""
利用\转义
```

**字符串拼接**：
```python
+  不能直接和数字类型拼接
"内容 %d %f %s" % (占位符对应的变量)
"内容{变量名称}，内容{变量名称}"
```

**数据输入**：
```python
input()
```

**简单语法**：
```python
if 条件:
    条件成立后的行为
elif 条件:
    行为
else
    行为
    
    
while 条件:
    内容

#无法定义循环条件，实际上功能只是java中的增强类
for 临时变量 in 待处理数据集:
    内容
    
#从0开始到num的数字序列
range(num) 
#从num1到num2
range(num1,num2)
#从num1到num2以step为步长
range(num1,num2,step)

continue
break
```